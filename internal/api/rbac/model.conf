[request_definition]
r = sub, obj

[policy_definition]
p = sub, obj

[policy_effect]
e = some(where (p.eft == allow))

[role_definition]
g = _, _

# Fuck
[matchers]
m = g(r.sub, p.sub) && g(r.obj, p.obj)