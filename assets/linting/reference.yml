# example only
run:
  timeout: 5m

linters-settings:
  staticcheck:
    checks: [ "all", "-SA4010", "-SA1019" ]

  gosimple:
    checks: [ "all", "-S1023" ]

linters:
  disable:
    - unused
    - nakedret
